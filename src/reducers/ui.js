const initialUiState = {
    menu: {
        activeItem: null,
    },
    measures: {},
}

export const ui = function(state=initialUiState, action) {
    switch (action.type) {
        // case expression:
        default:
            return state
    }
}
export default ui
